// This file acts as a configuration manager for the application.

/**
 * Toggles the application's mode.
 * - true: Development Mode. Enables settings, library management, and dynamic library selection.
 * - false: Production Mode. Hides settings and uses a fixed, hardcoded library for all AI features.
 */
export const IS_DEV_MODE = true;
